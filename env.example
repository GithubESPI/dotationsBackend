# Port de l'application
PORT=3000

# URL du frontend (pour CORS)
FRONTEND_URL=http://localhost:3001

# Configuration Azure AD
# IMPORTANT: Utilisez le Tenant ID spécifique (pas 'common') pour limiter l'accès aux utilisateurs du tenant
AZURE_AD_CLIENT_ID=your-azure-ad-client-id
AZURE_AD_CLIENT_SECRET=your-azure-ad-client-secret
AZURE_AD_TENANT_ID=your-azure-ad-tenant-id
AZURE_AD_REDIRECT_URI=http://localhost:3000/auth/azure-ad/callback

# Domaine autorisé (optionnel)
# Si défini, seuls les utilisateurs avec un email se terminant par @{AZURE_AD_ALLOWED_DOMAIN} pourront se connecter
# AZURE_AD_ALLOWED_DOMAIN=groupe-espi.fr

# Configuration JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=1h

# Configuration Session (requis pour passport-azure-ad)
SESSION_SECRET=your-session-secret-change-in-production

# Environnement
NODE_ENV=development

# Configuration MongoDB
# Format: mongodb://[username:password@]host[:port][/database][?options]
# Exemple avec Docker: mongodb://dotation:dotation_password@localhost:27017/dotation?authSource=admin
MONGODBURI=mongodb://dotation:dotation_password@localhost:27017/dotation?authSource=admin

# Note: Les scopes Microsoft Graph sont configurés dans la stratégie Azure AD
# Scopes par défaut: ['openid', 'profile', 'email', 'User.Read', 'offline_access']
# Pour ajouter d'autres permissions, modifiez src/auth/strategies/azure-ad.strategy.ts

# Configuration Jira Asset
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-jira-api-token
# Pour générer un API token: https://id.atlassian.com/manage/api-tokens

# Configuration Jira Assets (nouvelle API)
# Si ces variables ne sont pas définies, les variables ci-dessus seront utilisées (rétrocompatibilité)
JIRA_BASE_URL_ASSETS=https://your-domain.atlassian.net
JIRA_BASE_PATH_ASSETS=/rest/servicedeskapi/assets
JIRA_EMAIL_ASSETS=your-email@example.com
JIRA_TOKEN_ASSETS=your-jira-api-token

